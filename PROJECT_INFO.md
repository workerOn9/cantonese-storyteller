# Cantonese Storyteller Project

本项目包含粤语评书应用程序的MVP实现。

## 🎯 项目概览
一个跨平台粤语有声读物创建应用程序，使用AI语音合成技术。用户可以录制自己的声音来训练自定义模型，并生成个性化的有声读物。

## 📁 项目结构

```
cantonese-storyteller/          # 主项目目录
├── src/                        # React前端（TypeScript）
├── src-tauri/                  # Rust后端（Tauri命令）
│   ├── src/
│   │   ├── commands/          # Tauri IPC命令
│   │   ├── services/          # 业务逻辑服务
│   │   └── db/                # 数据库层
│   ├── migrations/            # SQLite模式迁移
│   └── icons/                 # 应用程序图标
├── backend-java/              # Java Spring Boot后端
│   ├── src/main/java/         # Java源代码
│   └── build.gradle           # Gradle构建配置
├── package.json               # NPM依赖
├── tsconfig.json             # TypeScript配置
├── vite.config.ts            # Vite构建配置
├── test-mvp.sh               # MVP测试脚本
├── README.md                 # 英文文档
├── README.zh.md              # 中文文档
└── index.html                # HTML入口点
```

## 🚀 快速开始

1. **安装依赖：**
   ```bash
   npm install
   cd src-tauri && cargo fetch
   cd ../backend-java && ./gradlew build
   ```

2. **运行应用程序：**
   ```bash
   # 终端1：前端 + Rust后端
   npm run tauri:dev
   
   # 终端2：Java后端
   cd backend-java && ./gradlew bootRun
   ```

## 🎯 MVP功能
- ✅ 30秒粤语语音录制
- ✅ 声音模型训练模拟
- ✅ 章节文本管理
- ✅ 实时合成进度
- ✅ 跨平台支持（Windows/macOS/Linux）
- ✅ 本地SQLite缓存

## 🛠️ 技术栈
- **前端：** Tauri 2.0 + React 18 + TypeScript
- **本地后端：** Rust（音频处理、文件管理）
- **远程后端：** Java Spring Boot + PostgreSQL
- **构建工具：** Vite + Cargo + Gradle

## 📋 下一步计划
详见README.md中的详细文档和路线图。

---
**注意：** 这是MVP实现。生产环境部署需要集成实际的TTS服务并增强错误处理。